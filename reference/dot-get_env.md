# Get an object from the genieBPC package environment (\`genieBPC_env\`)

Get an object from the genieBPC package environment (\`genieBPC_env\`)

## Usage

``` r
.get_env(thing_to_check)
```

## Arguments

- thing_to_check:

## Value

The object

## Examples

``` r
if (FALSE) { # \dontrun{
.get_env("pat")
} # }
```
